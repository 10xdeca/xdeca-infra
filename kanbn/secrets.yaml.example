# Kan.bn secrets (encrypt with: sops -e -i secrets.yaml)

kanbn_url: https://kanbn.enspyr.co

# Generate with: openssl rand -hex 32
auth_secret:

# Postgres - generate with: openssl rand -hex 16
postgres_password:

# SMTP (Brevo or other provider)
smtp_host: smtp-relay.brevo.com
smtp_port: "587"
smtp_username:
smtp_password:
smtp_from_email: noreply@enspyr.co
